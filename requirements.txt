# Core libraries
torch>=2.2.0
transformers>=4.41.0
datasets>=2.19.0
accelerate>=0.30.0
trl
trl[vllm]>=0.9.4
vllm>=0.4.0.post1
wandb>=0.17.0


# Optional speedups (uncomment if compatible with your system)
flash-attn==2.5.7

# Note: Ensure your custom `sparc` package (providing `sparc.prompt` and `sparc.validation`) 
# is installed or available on PYTHONPATH for training scripts to run.
sparc-puzzle

